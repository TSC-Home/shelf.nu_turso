/**
 * Auth session mappers.
 *
 * The original Supabase session mapper was removed when the auth layer was
 * replaced with a custom JWT + bcrypt implementation.  This file is kept as
 * a stub so any accidental imports do not cause build errors.
 */
export {};
